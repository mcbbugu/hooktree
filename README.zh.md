# hooktree

[English](README.md) · 中文

安装前从 lockfile 打印会执行的 install 生命周期脚本树。

```
node --import tsx src/cli.ts .
node dist/cli.js .
```

## 命令

```
hooktree [dir]
hooktree [dir] --json
hooktree [dir] --strict
```

Node 20+。MIT。
